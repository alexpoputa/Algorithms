var humanYearsCatYearsDogYears = function(humanYears) {
 // Create new variables and an empty array to store the result
 var cat =0;
 var dog =0;
 var arr = [];
 // For each year, add a certain value to cat and dog's years
 for(var i=1;i<=humanYears;i++){
 if(i == 1){
   cat = 15;
   dog = 15;
 }else if(i == 2){
   cat += 9;
   dog += 9;
 }else if(i > 2){
   cat += 4;
   dog += 5;
 }
}
  // Push the variables to the new array and return it
 arr.push(humanYears,cat,dog);
 return arr;
}
