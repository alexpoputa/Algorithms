/**
 * @param {number} x
 * @return {boolean}
 */
var isPalindrome = function(x) {
    x=x.toString();
  if(x[0]=='-'){
    return false;
  }
  for (var i=0, j=x.length-1; i<x.length, j>=0; i++, j--){
    if(x[i] !== x[j]){
      return false;
    }
    if (i==j){break;}
  }
  return true;
};
